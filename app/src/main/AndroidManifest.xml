<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.faj.project.donationapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".BankBill"></activity>
        <activity android:name=".ProductQtd" />
        <activity android:name=".MoneyDonation" />
        <activity android:name=".Cadastro" />
        <activity android:name=".Products" />
        <activity android:name=".BankInformation" />
        <activity android:name=".SignInAddressFragment" />
        <activity
            android:name=".Campaigns"
            android:label="@string/title_activity_campaign"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>